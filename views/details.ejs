<%- include("partials/header.ejs") %>
<h3>
  <%= post.user.name %> <%= post.reQuote ? `is Requote to
  ${post.reQuote.user.name}`: "" %>
</h3>
<h3><%= post.time %></h3>
<h2><%= post.title %></h2>
<% if(post.img) %> <img src="<%= post.img %>" />
<% if(post.reQuote) { %>
<a href="/?id=<%= post.reQuote.id %>">
  <img class="pfp" src="<%= post.reQuote.user.pp %>" />
  <h4><%= post.reQuote.user.name %></h4>
  <h4><%= post.reQuote.time %></h4>
  <h3><%= post.reQuote.title %></h3>
  <% if(post.reQuote.img) %> <img src="<%= post.reQuote.img %>" />
</a>
<% } %>
<button onclick="sendLikeRequest('<%- post %>')">Like: <%= post.like.total %></button>
<button onclick="sendPostRequest(undefined, undefined, undefined, '<%= post.id %>', '<%- post %>')">Repost</button>
<form onsubmit="sendPostRequest(this,'<%- post %>', '', undefined, undefined)">
  <div>
    <label for="title">Reply</label>
      <input type="text" id="title" name="title">
  </div>
  <div>
    <label for="img">Image URL:</label>
    <input type="text" id="img" name="img">
  </div>
  <button type="submit">Post</button>
</form>
<form onsubmit="sendPostRequest(this, '', '<%- post %>', undefined, undefined)">
  <div>
    <label for="title">Requote</label>
      <input type="text" id="title" name="title">
  </div>
  <div>
    <label for="img">Image URL:</label>
    <input type="text" id="img" name="img">
  </div>
  <button type="submit">Post</button>
</form>
<% replies.forEach((reply) => { %>
<a href="/?id=<%=reply.id %>">
  <h3><%= reply.user.name %></h3>
  <h2><%= reply.title %></h2>
  <h3><%= reply.time %></h3>
  <button onclick="sendLikeRequest(reply)">Like: <%= reply.like.total %></button>
  <% if(reply.img) %> <img class="pfp" src="<%= reply.img %>" />
</a>
<% }); %> <% if(post.img) %> <%- include("partials/footer.ejs") %>
